# Moved

The Terminal v0.5 app that lived here (`index.html`) is now the real
application and moved to its permanent home:

**`TradeW/apps/terminal/index.html`**

(git rename on 2026-07-16 — full history preserved). Run it from there with
`python -m http.server 3000`; see `apps/terminal/README.md`.
