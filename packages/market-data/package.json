{
  "name": "@tradew/market-data",
  "version": "0.1.0",
  "private": true,
  "description": "Shared market-data engine — provider/feed contracts, the single simulated market, the Dhan adapter, and the ingestion primitives. Consumed by services/market-data (ingestor), services/api (reads) and services/sentinel.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "ts-node -T scripts/verify-parser.ts",
    "//test": "The scrip-master parser check kept as-is, plus the vitest suite added with the 2026-08-17 reliability layer. Both, in that order, because a broken parser makes every other failure harder to read.",
    "test": "npm run verify && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@tradew/types": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "ts-node": "^10.9.2",
    "vitest": "^2.1.9"
  }
}
