{
  "name": "@tradew/market-data",
  "version": "0.1.0",
  "private": true,
  "description": "Shared market-data engine — provider/feed contracts, the single simulated market, the Dhan adapter, and the ingestion primitives. Consumed by services/market-data (ingestor), services/api (reads) and services/sentinel.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "ts-node -T scripts/verify-parser.ts",
    "test": "npm run verify"
  },
  "dependencies": {
    "@tradew/types": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "ts-node": "^10.9.2"
  }
}
