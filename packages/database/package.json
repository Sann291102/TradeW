{
  "name": "@tradew/database",
  "version": "0.1.0",
  "private": true,
  "description": "Single Prisma schema + migration history — the one schema owner for Postgres (ARCHITECTURE.md §1.4). Also owns the at-rest encryption format for BrokerCredential.accessToken (src/credential-crypto.ts).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "generate": "prisma generate --schema prisma/schema.prisma",
    "migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "migrate:deploy": "prisma migrate deploy --schema prisma/schema.prisma",
    "seed": "ts-node prisma/seed.ts",
    "admin:grant": "ts-node scripts/grant-admin.ts",
    "operator:create": "ts-node scripts/create-operator.ts",
    "execution:profile": "ts-node scripts/create-execution-profile.ts",
    "//agents:seed": "Create the NIFTY and SENSEX autonomous paper agents, DISARMED. Arming stays a separate console act on top of PAPER_EXECUTION_ENABLED.",
    "agents:seed": "ts-node scripts/seed-autonomous-agents.ts",
    "//credential:migrate": "Backfill: encrypt any BrokerCredential.accessToken still stored in plaintext. Dry-run by default — see scripts/encrypt-broker-credentials.ts.",
    "credential:migrate": "ts-node -T scripts/encrypt-broker-credentials.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.10.2",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
