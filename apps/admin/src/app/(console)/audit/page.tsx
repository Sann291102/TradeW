import { ModulePlaceholder } from '@/components/shell/ModulePlaceholder';

export default function AuditPage() {
  return <ModulePlaceholder title="Audit & Compliance" description="AI decisions, overrides, policy violations, user activity." />;
}
