import { ModulePlaceholder } from '@/components/shell/ModulePlaceholder';

export default function HealthPage() {
  return <ModulePlaceholder title="Engine Health" description="Agent status, queue depth, latency, memory, model usage, errors." />;
}
