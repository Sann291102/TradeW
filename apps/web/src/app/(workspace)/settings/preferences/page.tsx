import { PreferencesSection } from './PreferencesSection';

export const metadata = { title: 'Preferences — TradeW Settings' };

export default function PreferencesSettingsPage() {
  return <PreferencesSection />;
}
