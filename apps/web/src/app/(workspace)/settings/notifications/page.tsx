import { NotificationsSection } from './NotificationsSection';

export const metadata = { title: 'Notifications — TradeW Settings' };

export default function NotificationSettingsPage() {
  return <NotificationsSection />;
}
