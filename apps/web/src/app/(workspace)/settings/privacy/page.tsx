import { PrivacySection } from './PrivacySection';

export const metadata = { title: 'Privacy & Security — TradeW Settings' };

export default function PrivacySettingsPage() {
  return <PrivacySection />;
}
