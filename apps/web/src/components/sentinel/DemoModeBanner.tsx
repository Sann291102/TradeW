export function DemoModeBanner() {
  return (
    <div className="border-b border-amber bg-amber-bg px-5 py-2 text-xs text-amber">
      Demo data — backend API not reachable (sign in with a Sentinel-entitled account for live observations). The
      layout and copy below are exactly what the live desk produces.
    </div>
  );
}
