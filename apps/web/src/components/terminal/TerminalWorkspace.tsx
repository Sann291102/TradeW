/**
 * SUPERSEDED — Milestone 3 (Institutional Workspace).
 *
 * This static 3-column grid was the M2 trading-terminal layout. It has been
 * replaced on `/trade` by `components/workspace/WorkspaceDock.tsx`, a dynamic
 * dock (resizable, collapsible, movable, closable/restorable panels + layout
 * presets) rendering the SAME underlying panel content components
 * (components/terminal/panels/*, unchanged).
 *
 * The full original implementation is preserved at
 * archive/web-terminal-workspace-static-grid.tsx.txt per CLAUDE.md Rule 1
 * (archive, never delete). Nothing imports this file anymore — kept in place,
 * intentionally empty of exports, rather than removed.
 */
export {};
