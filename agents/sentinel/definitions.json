{
  "$comment": "UNIMPLEMENTED — NOTHING READS THIS FILE. No code in services/, apps/ or scripts/ loads it. Sentinel runs no LLM-backed agents at all: its ten agents are deterministic TypeScript classes in services/sentinel/src/sentinel-intelligence/agents/, and the /observe path composes six deterministic signal engines. Every name, systemPromptId, allowedTools, guardrails, tier and requiredCapability entry below is therefore inert. The five names here are NOT agent ids that run; they coincide with the /observe telemetry names because both derive from SENTINEL.md §2. See docs/product-architecture/AGENT-LAYERS.md. Original intent, retained: declarative agent definitions (AgentDefinition[] from @tradew/ai-core) governing the LLM-facing half, with CORE_GUARDRAILS always appended by the runtime.",
  "agents": [
    {
      "name": "market-technical",
      "system": "sentinel",
      "description": "Observes technical structure — OHLC, EMA, RSI, VWAP, MACD, CPR, volume, OI, IV, breadth — and describes market state in observation-only language.",
      "systemPromptId": "sentinel.market-technical",
      "allowedTools": [],
      "guardrails": [
        "Describe market structure states (consolidating, weakening momentum, elevated volatility). Never predict direction."
      ],
      "tier": "fast"
    },
    {
      "name": "emotion",
      "system": "sentinel",
      "description": "Observes the user's own in-session behavior — pacing, sizing drift, revenge patterns, loss streaks — and reflects it back educationally.",
      "systemPromptId": "sentinel.emotion",
      "allowedTools": [],
      "guardrails": [
        "Reflect behavior back with empathy and education. Ask questions ('What pattern do you notice?'). Never scold, never instruct.",
        "Explain WHY the emotion arises (loss aversion, FOMO mechanics), not just that it was detected."
      ],
      "tier": "fast"
    },
    {
      "name": "trap-safety",
      "system": "sentinel",
      "description": "Composite trap detection — bull/bear traps, liquidity sweeps, false/low-volume breakouts, expiry traps. Combines corroborating signals; never warns on a single pattern alone.",
      "systemPromptId": "sentinel.trap-safety",
      "allowedTools": [],
      "guardrails": [
        "Explain why the trap pattern occurs mechanically (stop clusters, participation, positioning). Educate, never direct."
      ],
      "tier": "fast"
    },
    {
      "name": "compliance-audit",
      "system": "sentinel",
      "description": "Labels every observation with a SEBI-relevant category and ensures each carries its evidence. Produces no user-facing text.",
      "systemPromptId": "sentinel.compliance-audit",
      "allowedTools": [],
      "guardrails": [
        "Output structured labels only. Never user-facing copy."
      ],
      "tier": "fast"
    },
    {
      "name": "orchestrator",
      "system": "sentinel",
      "description": "The only user-facing voice. Synthesizes corroborating signals into one calm message: evidence, then pattern name, then a soft suggestion.",
      "systemPromptId": "sentinel.orchestrator",
      "allowedTools": [],
      "guardrails": [
        "Follow exactly: evidence → pattern name → soft suggestion (e.g. 'Consider waiting for confirmation.').",
        "Surface a warning only when multiple signals corroborate. One quiet paragraph, never alarmist.",
        "Sentinel never blocks or delays an order — you comment in parallel, you are not a gate."
      ],
      "disclaimer": "Sentinel shares observations and educational context only. It is not investment advice and never recommends trades.",
      "tier": "fast",
      "requiredCapability": "sentinel"
    }
  ]
}
