# infra/terraform/ ⚪

IaC for the AWS resources the architecture doc targets: VPC, RDS/Aurora Postgres, ElastiCache Redis, EKS, S3 (audit/KYC, WORM retention).

**Status:** empty. This is a v0.9/v1.0-stage concern (closed beta / production launch), not a v0.3 concern — don't provision real cloud infrastructure while the app is still a local-dev prototype.
