# infra/k8s/ ⚪

Kubernetes manifests/Helm charts, one Deployment per `services/*`/`apps/*`, for staging and production on AWS ap-south-1.

**Status:** empty. Don't build this before `infra/docker/` proves the services work together locally — provisioning Kubernetes for code that hasn't run anywhere yet is premature.
