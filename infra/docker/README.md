# infra/docker/ ⚪

Local-dev docker-compose setup: `api` + `web` + `trading-engine` + `postgres` + `redis`.

Base this on the `docker-compose.yml` already present in `TradeW -(Setup & Paper)\TradeW-Setup-main\tradew-prototype\` rather than writing one from zero — it already covers the Node side; add `trading-engine` and `redis` services to it.

**Status:** empty, not yet written.
