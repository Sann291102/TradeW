# Superseded 2026-08-04 (monorepo env consolidation) — this service now
# reads the repo root .env at boot (see the dotenv call in src/main.ts and
# scripts/*.ts).
#
# For the full list of variables services/sentinel reads and their
# documentation, see /.env.example at the repo root.
