{
  "name": "@tradew/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "//test:integration": "The DB-backed Sentinel execution suite. Needs DATABASE_URL pointing at a migrated database; skips itself without one. Kept out of `test` because that suite promises to need nothing running — see vitest.integration.config.ts.",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "seed": "npm run seed -w @tradew/database",
    "verify:paper-execution": "ts-node --transpile-only -P tsconfig.json scripts/verify-paper-execution.ts",
    "//verify:autotrade": "The AutoTrade user journey over real HTTP: admin ARM -> the capability appearing -> user activation -> an order in the user's own book -> qualification -> LIVE ARM -> disarm. Boots the real AppModule; needs DATABASE_URL and a seeded plan catalogue. Substitutes only the two outbound network calls (Sentinel, the price bridge), so it runs without the feed.",
    "verify:autotrade": "ts-node --transpile-only -P tsconfig.json scripts/verify-autotrade-journey.ts",
    "verify:user-binding": "ts-node --transpile-only -P tsconfig.json scripts/verify-user-account-binding.ts",
    "prisma:generate": "prisma generate --schema ../../packages/database/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema ../../packages/database/prisma/schema.prisma",
    "verify:admin-routes": "ts-node --transpile-only -P tsconfig.json scripts/verify-admin-routes.ts"
  },
  "prisma": {
    "schema": "../../packages/database/prisma/schema.prisma"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/platform-express": "^10.4.15",
    "@nestjs/swagger": "^8.1.1",
    "@nestjs/throttler": "^6.5.0",
    "@prisma/client": "^5.22.0",
    "@tradew/ai-core": "*",
    "@tradew/database": "*",
    "@tradew/market-data": "*",
    "bcryptjs": "^2.4.3",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "dotenv": "^16.4.5",
    "nodemailer": "^9.0.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.8",
    "@nestjs/schematics": "^10.2.3",
    "@nestjs/testing": "^10.4.15",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.10.2",
    "@types/nodemailer": "^8.0.1",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
