# Superseded 2026-08-04 (monorepo env consolidation) — this service now
# reads the repo root .env at boot (see the dotenv call in src/main.ts).
#
# For the full list of variables services/api reads and their documentation,
# see /.env.example at the repo root. Copy it to a root .env and fill in
# real values; there is no longer a services/api-local variant to maintain.
