---
type: index
date: 2026-07-17
tags: [index]
---

# TradeW Knowledge Index

Read this first, every time — cheaper than searching, and search should still come before deep reasoning on any task (see [Rule 4](../../CLAUDE.md) in the project root). One line per note, grouped by folder. Update this file whenever a note is created or removed.

## Decisions
Architecture and project decisions, ADR-style — what was decided, why, what was rejected.
- [[Decisions/2026-07-17 - Obsidian Knowledge Layer adopted]] — why this vault exists, its scope, and how it relates to Sentinel's Postgres Brain
- [[Decisions/2026-07-17 - Genesis v2 blueprint added as new product-architecture docs]] — 9 new docs/product-architecture/ files (Learning Hub, Knowledge Graph, Continuous Learning Pipeline, TradeW Assistant, Subscriptions, Onboarding, TradingView Workspace, n8n catalog, summary blueprint); in-app knowledge graph reuses Sentinel's existing Brain, not new infra
- [[Decisions/2026-07-17 - Genesis v2 direction update (TRADEW-OS constitution + Research Vault)]] — direction update added TRADEW-OS.md (constitution, top of doc hierarchy), Research Vault (raw evidence vs validated knowledge, same store/different stage), Explainability principle, Workspace Continuity, Agent Architecture (n8n orchestrates agents not logic); TradeW AI→Sentinel auto-invoke reconciled as api-layer orchestration (no direct arrow, preserves ARCHITECTURE.md §9)
- [[Decisions/2026-07-18 - Market Data domain architecture review]] — 5-10yr schema review before any migration: Quote=latest-snapshot only (needs @@unique+source field, not previously enforced); Candle (new, needed by Charts AND Sentinel's Trap Detection — currently blocked without it); OptionMetrics separate from Quote (cardinality/write-pattern mismatch); Depth L2 never persisted (realtime-only); CorporateAction + Watchlist/WatchlistItem also net-new; 4 migrations sequenced, none applied; full doc: docs/product-architecture/MARKET-DATA-ARCHITECTURE.md
- [[Decisions/2026-07-21 - Sentinel Concept Knowledge Graph (living ontology)]] — **implemented.** Second knowledge graph, separate from this vault: 66 market concepts / 273 semantic relations in `knowledge-base/` YAML, projected into new `ConceptNode`/`ConceptEdge`/`ConceptObservation`/`ConceptPromotion` tables; closed 13-relation vocabulary with polarity+transitivity driving a best-first reasoning layer; runtime reinforcement kept strictly separate from canonical knowledge so reseeds never destroy learning; Sentinel proposes new knowledge but only humans promote it. Read its "three things called knowledge" table before touching `knowledge/`, `knowledge-base/`, or `GraphNode`
- [[Decisions/2026-07-21 - Sentinel reinstated as a TradeW workspace (decoupling reversed)]] — **binding. Read before touching anything Sentinel-facing.** TradeW is one ecosystem (Bloomberg Terminal / M365 / Adobe CC / Notion model); Sentinel is its flagship premium intelligence *workspace* and the AI layer beneath the platform, never a separate application. Marketing surface ≠ application architecture: a dedicated Sentinel landing page is fine, the rule binds from sign-in onward. Lists the platform systems that must never be duplicated per pillar, and why a change that requires amending the constitution to stop being a violation is a signal to re-check the change
- [[Decisions/2026-07-30 - Sentinel Learning Vault created (upstream authoring layer)]] — dedicated Obsidian vault at `knowledge/sentinel-learning/` as the authoring/research layer feeding the Brain; NOT runtime, NOT a replacement for `knowledge-base/`; 14 trading books at `docs/Trading Books/` are the initial knowledge source; ingestion pipeline is Phase 2
- [[Decisions/2026-08-12 - Cognition network (perceptors + four layers)]] — **read before touching `packages/ai-core/src/cognition/`, `services/api/src/cognition/`, or `/admin/cognition`.** 17 perceptors across five domains (market, application, assistant, learning, platform) feeding a four-layer network — L1 gate → L2 encode → L3 associate → L4 consolidate — whose weights are updated by **online reward-modulated Hebbian learning, not backprop** (the data is a slow trickle of weakly-labelled outcomes from a shifting distribution; every weight is a named pair an operator can read). Three things that will bite: **firing never moves a weight, only a scored outcome does**; **reward 0.5 is neutral, not 0** — encoding "unknown" as zero makes the network unlearn itself into silence; and **`NeuralSynapse` is excluded from the retention sweep on purpose** — it is the only irreplaceable table here and pruning it "for consistency" deletes everything learned. LangChain is honoured **structurally** (`langchain-adapter.ts` implements the duck-typed retriever/memory shapes with no import) because importing it would bypass `instrumentLlmProvider` and blank the console's cost figures. "Full application control" is implemented as perception + operator-gated proposals: no `execute` kind, and a market hypothesis never becomes a proposal at all
- [[Decisions/2026-08-15 - Sentinel-py personal strategy watcher (additive Python runtime)]] — **read before touching `services/sentinel-py/` or its `UserStrategy`/`WatchSession`/`WatchObservation` tables.** New Python/FastAPI service (port 4011), **additive** to the unchanged TS `services/sentinel`: the user writes their own text strategy, a deterministic (no-LLM) parser makes rules, an asyncio sweep watches live candles `IDLE→FORMING→CONFIRMED`, then in-trade R-multiple monitoring. Four load-bearing decisions: it reads/writes its **own tables via `asyncpg`, never Prisma** (Prisma still owns the migration — `ARCHITECTURE.md` §1.4); **no new WebSocket** (reuses the 30s `/notifications` poll → ~45s latency by choice, not a bug); a **compliance gate** (`app/notify/compliance.py`) blocks Buy/Sell/Entry/Target/Stop before anything leaves; the sweep is **`JobLease`-gated** so replicas don't double-notify but the single in-process loop dies with its process. Risk read from the adverse extreme, reward from the close (quick to warn, slow to claim progress). Datetime gotcha: keep everything tz-aware at the asyncpg boundary (`1b81dfa`)
- [[Decisions/2026-08-12 - Operator identity for the standalone admin console]] — **read before touching `services/api/src/admin/`, `AdminGuard`, `AdminAccessGuard`, `OperatorAccount`, or wiring `apps/admin`.** The standalone control plane gets its OWN identity store (`OperatorAccount`, separate from `User`) and a **composed** `AdminAccessGuard` — never a widened `AdminGuard`. The operator token (`ADMIN_API_TOKEN`) is required on both admin paths and **sufficient on neither**; the reverted shortcut that made it sufficient alone made `setAdmin`/`resolveProposal` record `'unknown'`. Two load-bearing keys: the operator assertion is signed with **`OPERATOR_JWT_SECRET`, not `JWT_SECRET`** (else it would be a valid product bearer token) and not derived from `ADMIN_API_TOKEN` (which is on the wire every request). Phase 1 (backend: model, service, guard, controller, CLI, 19 specs) and Phase 2 (apps/admin: `/api/auth/login` → server-side `services/api /admin/operator/login` → assertion **AES-256-GCM sealed** under a server-only `ADMIN_SESSION_SECRET` in an httpOnly cookie → proxy + SSE routes forward both `x-admin-token` and `x-operator-assertion`; old token-only session archived) are both **done and tested** (api 36 admin/auth, apps/admin 7 session). The browser never receives the assertion or `ADMIN_API_TOKEN`. Phase 3 also done + tested: the proxy is now **deny-by-default** (`adminProxyRoutes.ts` allowlist, not a wildcard) and `KnowledgeController` was moved off bare `AdminGuard` onto `AdminAccessGuard` (a gap Phase 1 opened — the console's knowledge pages would otherwise 401 on the operator path). **Invariant: every admin sub-surface uses `AdminAccessGuard`, never `AdminGuard` directly.** Remaining: Phases 4–6 (function-mapped UI migration from `apps/web/src/app/admin`, parity, archive). Our changes do not touch `apps/web`, but a **concurrent session** has separately deleted `apps/web/src/app/admin/*` and built the parallel `apps/admin/(console)/*` UI in the same working tree — verify that UI migration before committing the web console's removal. Before the path runs live: apply the `OperatorAccount` migration, set `OPERATOR_JWT_SECRET`+`ADMIN_SESSION_SECRET`, create an operator. **No header ever confers trust**
- [[Decisions/2026-07-21 - Sentinel decoupled into standalone marketing+app product]] — **⛔ REVERSED, do not implement.** Retained per Rule 1 for its still-accurate ground-truth audit findings; the direction is superseded by the note above. Originally recorded as "not yet executed" — Sentinel gets its own marketing site + app, zero shared nav to Research/Trade/Learning, same backend/auth/entitlements; creates a direct conflict with TRADEW-OS.md §1's "never separate products bolted together" that must be resolved by amending the constitution first; root cause of the old model traced to DESIGN-SYSTEM.md §3; full KEEP/UPDATE/DEPRECATE doc audit delivered as a chat artifact, no docs/code edited yet

## Patterns
Reusable engineering patterns, workflow improvements, development standards.
- [[Patterns/2026-08-16 - Sentinel reference dashboard band (what the mock asked for that the platform cannot know)]] — **read before adding a panel to `components/sentinel/dashboard/`, wiring a figure into `SessionStats`, or touching `lib/sentinel/sessionTimeline.ts` / `sessionStats.ts`.** A reference screenshot integrated as the band below the reading panel (feed · conditions · radar+emotion → session timeline → stats · actions · context). **Six of the mock's figures had no data source anywhere in TradeW** — FII/DII flow, global breadth, news impact, the forward calendar — and one was outright forbidden (position-size "recommended: 75%" is advice, Rule 2); each would have rendered as a confident constant on every load. Rule adopted: **an unavailable dimension is NAMED, not silently dropped**, so "measured as low" stays distinguishable from "cannot see this at all". The bug it surfaced: **the session timeline was ordered by APPEND, not by time** — the engine stamps a detection with its *bar* time and guidance with the *poll* clock (both correct, per the 2026-08-13 autonomy fix), so a 10:05 detection recorded on the 10:20 poll rendered after a 10:18 entry on a track read left-to-right as a chronology; sort on `at`, **never `time`** (a `HH:mm` string cannot order across midnight), ties keep input order because detections legitimately share a candle. Two arithmetic traps in the new stats aggregate: **performance is keyed by strategy, not watch** (its funnel already counts every watch, so per-watch fetching triple-counts one outcome) and **expectancy must be weighted by completed count** (else one lucky +3R outvotes thirty-nine at −1R: +1.00R displayed for −0.90R). **A rate never travels without its sample size** — `winRate` carries `decided` and is `null`, not `0`, when nothing has been decided. Also: "View all" → scroll (the feed is already server-collapsed, so a cut-off hides distinct events, not repetition), three deliberate poll cadences (45s `/observe`, 10s sentinel-py — the sweep runs every 15s), and `stopPrice`/`targetPrice` renamed to `invalidationPrice`/`projectedPrice` at the service boundary. 390/390 web + 248/248 py; **not driven in a browser** (auth + capability + an active watch + five services + market hours)
- [[Patterns/2026-08-16 - Sentinel charts on the bars the engine reads (the chart and the agent disagreed)]] — **read before touching `SentinelLiveCharts`, `lib/sentinel/chartFocus.ts`, `StrategyTimelineFeed`'s selection, or `build_timeline` in sentinel-py.** The three-chart panel (index + CE + PE) had been **dead code with zero importers** since the 2026-08-11 dashboard redesign, and while it lived it drew a hardcoded 5m index / 1m contracts while the sweep evaluated on `rules.timeframe` — the user and the engine were looking at different bars. Re-wired to the watch selected in the feed, on the series the engine actually receives. The load-bearing discovery: **the Dhan bridge substitutes silently** (`INTRADAY_INTERVAL[interval] ?? '5'`), so a strategy saved as `3m` is being evaluated on **5m bars right now with no error anywhere in the chain** — `resolveSeries()` reproduces that fallback deliberately and says so out loud, because drawing the requested 3m would show bars the engine never read. Also: `poller.py` has always recorded `pullback`/`vwap`/`flip`/`flag`/`zone`/`liquidity` on every sweep and **nothing had ever read them back out** — now surfaced, with staleness stated *above* the numbers (measurements come from the newest *readable* sweep, "could it read" from the newest sweep of any kind, so a dead bridge cannot leave old levels looking current). Exactly one tile is marked "Sentinel reads this", mirroring `_candles_for`: a strike watch reads the contract, a strikeless watch reads the index. The engine default (15m) stays in one place — the service sends `timeframe: null` rather than a second copy. **Not driven in a browser**: the live path needs the full stack plus an active watch, and no watch has ever run — which is the zero-samples problem this unblocks but does not itself solve
- [[Patterns/2026-08-14 - Tara chart drawing layer (series primitive, FVG detection, the concept collision)]] — **read before touching `apps/web/src/lib/charts/`, `TradeChart.tsx`, or adding a command whose vocabulary overlaps `concepts.ts`.** Weeks 1–2 of the Tara build order. Rendering is a **Series Primitive** (`series.attachPrimitive()`), which makes **lightweight-charts 4.1 a hard floor** — an overlay canvas would re-implement coordinate/DPR/clipping and still lag a frame per pan. Geometry is pure and injected because **every failure mode here renders as a confident annotation stating a level nothing detected**: a missing price is fatal, a missing time is not, and a trendline is stricter than a zone because clamping an endpoint changes its slope. Three things that will bite: **mitigation must scan from the bar AFTER the third candle** — `c.low` *is* the top of a bullish gap, so including it marks every gap mitigated on discovery and the chart draws nothing forever, with no error; **`router.ts` resolves commands before concept questions**, so "mark the fair value gaps" and "what is a fair value gap" hit the same matcher and the detector must defer using `concepts.ts`'s *own* exported `EXPLAIN_RE` (a second copy is how the answered-by-navigating regression returns); and adding this grammar **converted a dormant hard-boundary gap into a live one** — `IMPERATIVE_TRADE_RE` was start-anchored, a bare "and" only splits on known verbs and "buy" isn't one, so *"show me the fvg and buy 50 lots"* executed its innocent half and dropped the order. Also: erasure is tag-scoped and replace-not-merge (a mitigated gap must *vanish*), drawings carry a `seriesKey` so NIFTY 15m zones can't render on RELIANCE 1D, detection runs in the executor because **the pure resolver has no candles and must name a detector rather than invent coordinates**, and Week 4's indicator overlays **extend `lib/sentinel/indicators.ts`** rather than adding a package. Verified by 281/281 tests + typecheck; **not yet driven in a browser**
- [[Patterns/2026-08-13 - Sentinel autonomy (the browser was the heartbeat)]] — **read before touching `MarketWatchService.register`/`expiryFor`, `SentinelIntelligenceService.watchSymbol`/`scheduleCorpusWarmup`, or `StrategyEngineService.scan`'s timestamps.** Corrects the 2026-08-04 watch notes: the loop was built, enabled and running, and had swept **zero times** on a live trading day because `watch.register()`'s only production caller was `/intelligence/reason` — a route no app calls — so every tick exited at the `no-symbols` guard before the `sweeps` counter incremented. Two compounding faults: `indexOnBoot` was declared, parsed and **read by nothing**, so the corpus stayed at 0 documents and `reasonInBackground` declined every sweep by design; and `detectedAt` was the `/observe` request clock, so every detection from one poll shared the poll's timestamp (three setups all reading "14:59"). Fixes: `/observe` registers via a new `watchSymbol()` delegating to the existing `register()`; a watch is held to the **market close** rather than to a TTL the 45 s dashboard poll was silently refreshing (the TTL was not a cost control, it was a browser dependency — the two real controls, `isTradingTime` and `watchMaxSymbols`, are untouched); the corpus warms at boot **deferred** (`setTimeout(5s).unref()`), idempotent at three levels → 223 docs / 2229 chunks / 66 concepts with nobody calling `/reason`; and market time (`detectedAt` = newest bar, via new `latestBarAt()`) is split from execution time (`observedAt` = scan clock). New `lastTickAt`/`lastSkipReason` exist because `lastSweepAt: null` + `sweeps: 0` read identically whether a loop is dead or idle — the ambiguity that hid this for a day. Records what was **not** verified live (`sweeps`/`occurrencesRecorded`/`reasoningRuns` need the market open; the work landed after the close) and why intra-scan detections still legitimately share one bar timestamp
- [[Patterns/2026-08-12 - Sentinel event contract (notifications consume a gated event, never the response)]] — **read before adding any Sentinel delivery channel (email/push) or any field to `SentinelEvent`.** Sentinel observations now persist as durable notifications instead of living only as long as a browser tab. The load-bearing decision: a notification consumes a **validated event derived after the publication gate**, never `ObserveResponse` itself — the response is a *dashboard* payload carrying `sideInFocus`/`optionContext` because the workspace shows evidence in context, while an event arrives on a lock screen with none of it. `SentinelEvent` therefore has no side/bias/strike field and `deriveSentinelEvents` is never *handed* one (`sideInFocus` is in scope at the call site and deliberately not passed) — the direct lesson of the 2026-08-11 CE-fabrication gotcha, where every defect was a derivation recovering a side from something that never carried one. Asserted at **both** ends (emitter and dispatcher) and each guard confirmed to fail when violated before being claimed. Also: why the dedupe key shares the timeline's vocabulary but needs a *durable* window (an in-memory set re-notifies everyone on every deploy), why emotional risk is **banded** rather than thresholded, why `NOTIFIABLE_STATES` is narrower than `GUIDANCE_STATES` (notifying on WAIT_AND_WATCH makes the channel a heartbeat), and that this is still **poll-driven** — durable but not yet generated with the tab closed
- [[Patterns/2026-08-11 - Landing page as decision brief + mascot as shared agent identity]] — **read before editing `components/landing/` or either AI trigger.** The landing page is now a ten-section decision brief (what you get → surfaces → assistant → Sentinel → curriculum → onboarding → pricing → security → FAQ), with the binding rule that **every figure on it traces to a product-architecture doc** and the two pricing honesty lines (orders are never gated by a plan; checkout isn't built, so nothing can be charged) are load-bearing, not fine print. Also records why the FAQ uses native `<details>` rather than a JS accordion, the `curl | grep opacity:0` check that must be repeated after any landing edit (ten new sections = ten new chances to reintroduce the invisible-landing bug), and why the mascot needed a **second** component rather than a resize: `MascotMark` is monochrome `currentColor` because it sits inside a filled teal button where the full mascot's `--card` chassis vanishes. All three assistant triggers (FAB, dock header, TopBar) were swapped together — `SparkleIcon` stays, it's still used by ChartPanel/OptionChainTab/search
- [[Patterns/2026-08-06 - Sentinel pre-live-validation hardening (two gates collapsed into one)]] — **read before touching `StrategyAdvisorService.sideInFocus` or `tsconfig.scripts.json`.** The P0 audit defect: `sideInFocus` gated on `confidence.meetsThreshold` alone, a **second weaker gate** beside the four-condition one, so a directional read could surface with a mandatory rule unmet and zero corroboration. Also: why `tsconfig.scripts.json` typechecked **zero** files for months (`exclude` is inherited through `extends` and overrode `include`) and the 25 latent errors that surfaced when fixed; date-based eviction over a market-close hook for the lifecycle map; `market-clock.spec.ts` finally running (+28 tests); and `warn` is **not** a Tailwind token — `amber` is. Runbook: `docs/SENTINEL-LIVE-VALIDATION-RUNBOOK.md`
- [[Patterns/2026-08-06 - Sentinel Phases 2-6 (behaviour, lifecycle, calibration, annotations, cross-validation)]] — **read before touching `intelligence/market-structure.ts`, `strategy/`, `improvement/adaptive-calibration.service.ts` or `orchestrator/institutional-cross-validation.ts`.** Market behaviour (break-of-structure vs change-of-character; a sweep is defined by the CLOSE, not the wick); a SECOND per-strategy lifecycle beside the session state machine (invalidation checked first, or a setup records a success on the tick it failed); the adaptive loop that existed but **was never scheduled** — `run()` was reachable only from an HTTP endpoint — now per (regime, strategy), which needed `recordOccurrence` to stop dropping `signal.data`; reasoning rendered through the existing `ChartAnnotation` contract; and cross-validation across five *evidence dimensions* where **abstention is not neutrality**. Records a signal double-count the type-checker caught, and three external blockers (no live feed, empty option-chain/news, unlicensed TradingView library)
- [[Patterns/2026-08-06 - Sentinel publication gate (four conditions, not one threshold)]] — **supersedes the ≥85% rule in `SENTINEL_MASTER_PLAN.md` principle 3.** Publication now requires four conditions (≥70% adaptive confidence AND all mandatory confirmations AND no conflicting evidence AND ≥2 independent corroborating sources); the threshold went *down* while the gate got *stricter*, and the note explains why 85 was doing structural filtering bluntly. Records why the threshold stays fixed while weights adapt, why a request may raise but never lower it, why Engine 2's silence is not disagreement, why the leading detection can't corroborate itself, and that `services/api` forwards new response fields for free via `return res.json()`
- [[Patterns/2026-08-05 - Sentinel workspace premium redesign (two-column rail layout)]] — presentation-only `/sentinel` redesign to a two-column institutional layout (market read left, Sentinel's commentary in a right rail); same `useSentinel`/`/observe` contracts and entitlement gate throughout. **Records two silent design-token misuses worth grepping for repo-wide:** `bg-surface` (no such key in the Tailwind preset — renders no background, no warning) and `/opacity` modifiers on color tokens (the preset maps colors to `var(--token)`, not rgb channels, so `bg-up/15` is a no-op; use the `*-bg` tint tokens). Also: why the decorative candles are fixed geometry rather than market data, why an unknown market-context dimension gets a double-width tile, and why the reference's violet Premium chip ships in brand teal as a real entitlement readout
- [[Patterns/2026-08-05 - Learning homepage premium redesign (honest data-gap handling)]] — presentation-only `/learning` redesign to match a course-platform reference screenshot; the worked pattern for reference-UI fields the backend doesn't have (XP/certificates/hours/live-sessions → honest "coming soon", never fabricated) vs. fields that only *look* missing (weekly streak calendar is fully derivable from `streak` + `lastActivityAt`; "next lesson" needed a stale-type fix, not new data — caught live as a wrong "Next: Liquidity" label right after completing that lesson)
- [[Patterns/2026-08-05 - Portfolio module rebuild (T+1 settlement lifecycle, snapshot+backfill Performance)]] — **read before touching `services/api/src/sim/{settlement,holdings,trade-history,performance}.service.ts` or `apps/web/src/components/portfolio/`.** Rebuilt `/portfolio` into the full broker spec (Summary/Holdings/Positions/Orders/Trade History/Performance/Position Management) on the existing OMS, no mock data. Real T+1 settlement (`Position.settlesAt`/`closeReason`, new `Holding`/`HoldingSettlement` models, `SettlementService`) — CNC only, position-level not per-lot. Trade History pairs entry/exit algebraically from `applyFill`'s own formula, which caught a real latent bug: a close-and-flip fill's `Trade.quantity` isn't the closing quantity, fixed with new `Trade.closedQuantity`. Performance is a hybrid EOD `PortfolioSnapshot` + on-read backfill (`DiaryEntry.source` marks which); `today()` never reads a snapshot; no `1D` endpoint exists (no intraday tick storage) and the Diary has no generated narrative (out of scope, would mean fabricating text). Also: why `netWorth` had to fold in `holdingsValue`, and that live order-flow verification hit a real environment constraint — the Dhan live-feed bridge died from what looks like multiple concurrent sessions competing for the 5-connection-per-account cap
- [[Patterns/2026-08-05 - Sentinel multi-strategy selection (Phase 2 of reasoning consolidation)]] — **Phase 2.** `selectedStrategyIds: string[]` (new, preferred) alongside deprecated singular `selectedStrategyId`; `ObserveResponse.strategyAdvices[]` alongside unchanged singular `strategyAdvice`; `StrategySelector.tsx` rewritten as a pill/checkbox multi-select. Verified via 19/19 real-DI-graph `verify:runtime` assertions + browser E2E. Also flags a likely-live Anthropic key sitting in `ADMIN_API_TOKEN`
- [[Patterns/2026-08-11 - Razorpay payments, EOD email, VPS deploy]] — **read before touching payments, the EOD job, or the prod compose.** Razorpay seam (raw REST+HMAC, no SDK; fulfilment ONLY via `EntitlementsService.activate`; idempotent on payment id; trust=signature not browser; webhook needs `rawBody:true`; sells Sentinel Pro only). EOD summary email reuses the PerformanceService leader-election sweep, 4 anti-spam gates, OFF by default. Google login was config-only (creds unset). Deploy: single VPS + docker-compose; operator-only admin via `docker-compose.admin.yml` (loopback+SSH tunnel, never public) + new `apps/admin/Dockerfile`; runbook `infra/docker/DEPLOY-DEV.md`.
- [[Patterns/2026-08-15 - Sentinel pre-payment page (day-length terms, durable claims, the shell that owned the session)]] — **read before adding a catalog item, a public route, or any page outside `(workspace)`.** Public `/sentinel/pricing` + a paid 7-day trial (₹275, **derived** from the one-month term, never typed — a hand-typed trial survives a price rise as a discount nobody approved). Three things that will bite: **`TRIAL_ITEM` is deliberately not in `CATALOG`** because `/checkout` divides by `months` and a zero-month row renders **₹Infinity/mo** on a screen this change never touched; **once-per-user must be a durable `UsageCounter` claim**, since enforcing it by looking for a *live* subscription makes the offer repeatable the moment the first lapses (the unique constraint IS the lock — `create`, never `upsert`, P2002 verified against the real DB); and the bug no test could see — **`AppFrame` is the only route-level caller of `sessionStore.init()`**, so a page outside `(workspace)` leaves `status` at `'idle'` forever and a signed-**in** user is shown "Sign in to check out" and bounced by their own Checkout button. Also: `fulfil` restates `trialEndsAt` from the term PAID FOR (not `Plan.trialDays`, a seed value) and `bg-teal/5` is *still* a silent no-op that renders solid teal. **Superseded same day on the frontend: the route became a VIEW** — `/sentinel` renders pricing-or-workspace off the capability, no redirects, route archived. The reframing: **the pricing page is a state of the workspace, not a place.** A two-way branch on `hasCapability` flashes the sales page at paying subscribers on every load — it needs a **third state** for the unresolved session; and check where an entitlement gate sits *relative to the hooks it gates* (this one sat below `useSentinel()`, so unentitled accounts fired `/observe` calls that could only 403)
- [[Patterns/2026-08-11 - Transactional email + in-app notification wiring]] — **read before touching outbound email, the notification drawer/bell, or the Sentinel live-feed poll.** Whole-app email + notification pass reusing existing infra: new `mail/templates.ts` (branded `{subject,text,html}` builders — `otpCode`/`loginAlert`/`passwordChanged`/`eodSummary`, table-based, escaped) wired into real auth flows fire-and-forget; new `notificationSound.ts` (synthesized WebAudio "TradeW mark", no asset) + `NotificationSync.tsx` (30s poll → store, chime on new, backlog-primed) unifying bell/drawer/page on one store slice; `useSentinel` live feed rings on new observation text. Gotchas: `NotificationCategory` has no security/login type (login→email only, not in-app); `ObserveResponse` has no `headline`; Gmail rewrites `From:` unless verified alias. Sender = `admin@tradew-setup.com`
- [[Patterns/2026-08-05 - Sentinel reasoning-engine merge (Engine 1 reads Engine 2 as corroboration)]] — **Phase 1 of the reasoning consolidation.** `SentinelOrchestratorService.decide()` reads `SentinelIntelligenceService.latestRun()` as a read-only corroboration signal (no synchronous Engine-2 invocation); new `ObserveResponse.crossValidation` field, never mixed with `synthesis`; disagreement never suppresses Engine 1; byte-identical output when no cached run exists. 144/144 tests pass (5 new)
- [[Patterns/2026-08-04 - SentinelIntelligence continuous reasoning (the watch asks the questions)]] — **read before changing `MarketWatchService`'s trigger policy or `reason()`'s signature.** The watch now asks the engine a question whenever a new setup forms, so noticing a setup and understanding it are no longer separated by whether a trader was at the screen. Records a cost estimate that was **wrong** and nearly cost a phase: the pipeline is fully deterministic — no LLM on any path — so a run's real cost is its snapshot fetch, which the sweep has already paid; reusing it makes background reasoning free at the data layer and stops the detection and the reasoning about it reading different ticks. The load-bearing decision is a filter deliberately *not* applied: pre-filtering on live performance looks like free savings and would make the risk-elevated exemption unreachable from the background path, using the gate as a cost optimiser for the thing it gates. Also: the TTL loop avoided by `register: false`, the DI cycle avoided by `setReasoner()` over `forwardRef`, why a cold corpus declines rather than ingesting from inside a timer, and why background runs carry no trader's position data
- [[Patterns/2026-08-04 - SentinelIntelligence continuous market watch (polling, not a sixth WebSocket)]] — **read before adding any background loop to `services/sentinel`, and before wiring `packages/market-data`'s `MarketFeed` into it.** Corrects a belief this vault helped spread: neither Sentinel engine ever watched continuously — both pull one snapshot per HTTP request and the web client polls neither, so a setup that formed and resolved between refreshes never existed to the system. Adds a watch loop over *the symbols traders actually have on their board* (registered from the request path, TTL-lapsed) rather than the ~219-symbol universe. Records why a Sentinel-side WebSocket is a production risk despite `DhanMarketFeed` already existing (Dhan allows 5 connections per account and evicts the oldest with code 805; `services/market-data`'s ingestor is a documented singleton — a sixth consumer could evict it), and why polling the bridge's `/quotes` reads genuinely push-fed ticks anyway. The re-record cooldown is load-bearing, not hygiene: without it one hour-long setup counts as dozens of occurrences and certifies itself to the live-performance gate. Also: why the weekday check lives locally instead of in `market-clock.ts`, why detection is continuous but reasoning stays on demand, and two pre-existing faults found on the way (`market-clock.spec.ts` never runs; `@tradew/ai-core` ships unbuilt)
- [[Patterns/2026-08-04 - SentinelIntelligence live-performance gate (books must be proven live)]] — **read before changing any surfacing gate in `services/sentinel/src/sentinel-intelligence/synthesis/`.** The gate is three conditions now, not two: a *directional* read must also name a pattern with a live-market track record (≥8 outcome-tagged occurrences in the Brain, reusing `StrategyIntelligenceService.MIN_SAMPLE` rather than inventing a second floor). Records the hole the first two gates structurally could not see — confidence and corroboration both measure agreement among agents reading the *books*, and neither measures whether the books are right about *this instrument* — and why `risk-elevated` is exempt (a warning makes no "this setup works" claim, and trapping a corroborated safety signal behind a track record inverts Sentinel's mandate). Also: why the gate stays pure/synchronous with the async lookup hoisted to the caller, why it fails closed on a Prisma outage, why the env toggle is opt-out, and that on a cold Brain directional silence is the gate working. This is the first read SentinelIntelligence does from the store the *orchestrator* writes, so both engines are judged against one shared record
- [[Patterns/2026-08-03 - SentinelIntelligence (second reasoning engine, citation-grounded)]] — **read before touching `services/sentinel/src/sentinel-intelligence/`, and before assuming Sentinel has one orchestrator.** A second engine beside the untouched orchestrator, over the same deterministic substrate: understand → decompose → 10 agents → cross-check → gate at ≥70% **and** ≥2 corroborating agents, where neither substitutes for the other. Adds a fourth thing called "knowledge" (a citable BM25 corpus, 194 docs → 2085 chunks, deliberately lexical so the citation guarantee holds with no Postgres and no embedding provider), and makes that guarantee *structural* — `VerdictBuilder.knowledge()` cannot be called without citations, and the cross-checker drops uncited verdicts rather than flagging them. Records why `risk-elevated` must stay off the directional axis, why the vocabulary enforcer mangled its own "not advice" disclaimer (enforce derived text, append reviewed constants after), the flat-vs-nested provider registration that keeps one Postgres pool, and five bugs that each looked fine on the page. Also: sentinel's **first** vitest runner, 89 tests
- [[Patterns/2026-08-03 - Test infrastructure pass (runners made discoverable, money math covered)]] — **read before adding any test, and before touching `services/sentinel/src/market-clock.ts`.** The repo's test problem was discoverability as much as breadth: three bodies of real passing assertions gated nothing (web's config hardcoded a single filename and skipped `optionChain.test.ts`; sentinel had 99 files and no runner at all, only ts-node harnesses; the Dhan binary-parser round-trip was reachable only as `npm run verify`) and **no CI workflow ran any test**. Adds root `npm test` across all workspaces, sentinel's first vitest config, a CI workflow (which must `prisma generate` first — no postinstall hook exists), and 62 new tests over the three highest-risk paths: `applyFill` position/P&L math, `EntitlementsService.check` quota+grace+override precedence, and `AuthGuard` (there is no `APP_GUARD` — the decorator is the whole boundary). Records why sentinel's weekend/holiday-blind market clock is pinned by *passing* tests rather than fixed, and that those assertions are meant to flip when clock-unification lands

- [[Patterns/2026-07-29 - Broker OAuth ownership and third-party content boundaries]] — **read before touching `services/api/src/broker/`, the news feed, or the `/feed` proxy.** The broker credential was one global row justified by a schema comment ("exactly one operator account behind the feed") that nothing enforced, so any account completing consent overwrote the platform's broker session — a security property in a comment is a property that does not exist. Records how to bind an OAuth callback when the provider does not echo `state` back (hash-stored CSPRNG state, strict match when present, sole-live-flow fallback, refuse ambiguity rather than guess), why the state must be consumed *before* the token exchange, that `href` is a script sink React does not close (allowlist http/https, validate the raw string because the URL parser strips TAB/LF/CR), and why `:path*` onto an unauthenticated service is a standing exposure regardless of what is exposed today. Honest limits: CSP keeps `unsafe-inline`, `accessToken` is still plaintext at rest, migration written but not applied
- [[Patterns/2026-07-26 - TradeW AI assistant control layer (Comet-style app control)]] — **read before touching the assistant.** Phase 1 makes `FloatingAI` take control of the app on command (Comet-style), resolving navigation/contracts/panels/layouts/theme with NO LLM call — pure resolution split from React execution so the grammar is testable. The Trade workspace's `?symbol&strike&type&expiry` deep link already existed, so no new rendering path was needed. Records the parsing bugs that only appeared in-browser ("NIFTY **50** 24300" → strike 50; bare "21st July" → a 2027 expiry; `call for July` refused as a tip; `open` in the allowlist made the remit fence meaningless), why guard order is a contract (boundaries above command resolution, remit fence below), and the 2026-07-26 call that Sentinel is navigable but never narratable. Also: the client-side fence is NOT a guardrail once Phase 2 generates prose
- [[Patterns/2026-07-26 - Sentinel Master Plan integration (12 modules into the existing service)]] — **read before touching Sentinel.** Where each of the Master Plan's 12 modules landed in the existing service, and the decisions not to re-derive: two independent surfacing gates (confidence ≥85% for guidance, composite ≥0.7 for risk warnings) rather than one; session state keyed `userId::symbol::istDate`, never global; strategies as declarative rule *names* so user YAML can't inject logic; Module 12 replays through the live engines (`composeSnapshot` + `scan`) with VIX/breadth/chain held null to avoid look-ahead; vocabulary enforced on *output*, not just the prompt. Also lists what was deliberately not built (no forward news calendar, no order-book depth — both stated in the evidence rather than faked)
- [[Patterns/2026-07-17 - Knowledge Workspace]] — in-app `/knowledge` vault viewer (services/api module + apps/web page): FS-backed, no DB, SSE live updates, snapshot-diff poller, dev-gated + authed
- [[Patterns/2026-07-18 - packages-ui foundation (tokens, preset, transpilePackages)]] — Phase 1 M1: how the shared design system is wired (source-only via transpilePackages, tokens extracted verbatim from canonical HTML, Tailwind preset, dark-first default, buttonClasses recipe, motion module)
- [[Patterns/2026-07-18 - M2 terminal conversion (shell, widgets, terminal slots)]] — Phase 1 M2: HTML terminal→React (path-aware AppFrame shell around existing pages without moving them, config-driven sidebar, dashboard widgets, terminal slot panels, lazy loading, layout reconciliation, trade-page archive decision)
- [[Patterns/2026-07-18 - M3 dockable workspace (zustand store, dock engine, command palette)]] — Phase 1 M3: zustand store + hydration-safety pattern, theme no-flash script, 5-zone dock (not free-floating windows), command palette = unified global search w/ modular+stub providers, state ownership migrated off local useState, browser-automation verification gotchas
- [[Patterns/2026-07-18 - M4 Step1 real auth session (sessionStore, AppFrame remount gotcha)]] — Phase 2 M4 Step 1: real /auth + /entitlements wiring, zero new backend code; sessionStore deliberately unpersisted; AppFrame-doesn't-remount-on-navigation bug found+fixed in login/signup; entitlement UI verified via real admin-API grant+revert round-trip
- [[Patterns/2026-07-21 - Market data Phase 1 (ingestion runtime, pure reads)]] — **read before touching market data.** The read path inverted: `services/api` no longer generates quotes (forced by Dhan's 1-req/sec quote cap), `services/market-data` became the sole writer and is a singleton (broker feed connections are a per-account resource), and the two divergent simulators collapsed into `packages/market-data`. Includes four scrip-master facts the Dhan docs omit, the `Instrument.symbol` uniqueness constraint that bounds future imports, and the `nest build`/`prisma generate` gotchas
- [[Patterns/2026-07-24 - EMA-cross backtest engine (Sentinel, real Dhan data)]] — **first backtesting capability** (`npm run backtest:ema`), observation/education only. Reuses `ema()` + the `getCandles` contract so the data substrate is swappable. Key findings: the simulator's constant-drift `simulateCandles` makes only 8–16% of bars bullish → **useless for validating long strategies** (use the real Candle table); 3 months of real 15m/1d Dhan bars exist for NIFTY/BANKNIFTY/FINNIFTY/RELIANCE/COALINDIA; the bare "just EMA" rule is breakeven-to-losing, and the fresh-cross filter flips indices positive but not stocks
- [[Patterns/2026-07-24 - Sentinel live data across the full universe]] — **Sentinel reads live Dhan data for the WHOLE universe now**, not just 5 backfilled symbols. `CandleMarketDataProvider` resolves live-feed bridge (`/candles`, `/quotes` for breadth+VIX) → Candle table → simulator (last resort). Verified GOLD/INFY/TATAMOTORS/NIFTY match the feed exactly. Env-gated (`SENTINEL_LIVE_FEED_URL`); pragmatic bridge pending Phase 4 ingestion; token expires ~15:21 IST daily. Frontend marks all markets live
- [[Patterns/2026-07-23 - Sentinel market selector + event-driven safety feed]] — user-centric "market head" selector (search+dropdown over indices/commodities/F&O, real-data dot) drives the whole `/sentinel` workspace via `useSentinel(symbol)`; Live Safety Feed made event-driven with a per-card `pushworthy` flag (surface genuine setups/behaviors, keep full history in the timeline) — the reusable "notify on real signal, not every tick" shape
- [[Patterns/2026-07-23 - Candle table + Dhan backfill (Sentinel on real data)]] — **Migration 2 executed + Sentinel on real data.** `Candle` table shipped (append-only OHLCV, `@@unique([instrumentId,timeframe,bucketStart])`); `backfill-candles.ts` (`npm run backfill:candles`) pulls real Dhan history (verified request/response shapes for `/v2/charts/historical` + `/intraday`, epoch-seconds UTC, closed-market-safe); `CandleMarketDataProvider` binds it to Sentinel's `getCandles` with simulator fallback. Proven: `/observe` NIFTY price == last `source='dhan'` candle close
- [[Patterns/2026-07-18 - Market Data Migration 1 executed (Quote revision, baseline established)]] — Quote revision applied as 2 migrations after live-DB diff caught pre-existing drift (schema.prisma had columns no migration ever shipped); `prisma migrate dev` unusable non-interactively, hand-authored SQL + `migrate deploy` instead; full baseline doc created, flags Sentinel's ephemeral candle simulator as a Migration 2 design question

## Gotchas
Debugging discoveries, bug resolutions, lessons learned — things that cost time once and shouldn't cost time twice.
- [[Gotchas/2026-08-12 - Nest DTOs must be declared above the controller]] — **read before adding a `@Body()`/`@Query()` DTO to any Nest controller.** A DTO class declared *below* its controller is still in its temporal dead zone when `emitDecoratorMetadata` evaluates `design:paramtypes` at class-definition time, so the API dies at import with `ReferenceError: Cannot access 'X' before initialization`. `tsc --noEmit` passes and all 281 tests pass — **no test instantiates the Nest context, so only actually starting the service catches it.** The same boot exposed a second trap: `AdminService` is provided twice (by `AdminModule` and directly by `ControlModule`, deliberately, to preserve the console's no-exports boundary), which only works while its constructor needs nothing but the `@Global` `PrismaService` — injecting a second service into it takes down the *control plane* at boot. Live in-memory state goes on the controller; `AdminService` stays Prisma-only
- [[Gotchas/2026-08-11 - Sentinel feed fabricated a CE direction on signals that had none]] — **Rule 2 class, read before touching `SafetyCard.tsx`, `extractSafetyFeed` or `sideInFocus`.** Four stacked defects made the feed claim Sentinel favoured *calls* on evidence that said the opposite: the CE/PE badge was recovered by substring-matching card prose and **defaulted to "CE CALL"** for anything not matched as PE (a bearish CPR breakdown got a bullish badge; "O**PE**N range" got PE PUT); a *neutral* bias was coerced to bullish, manufacturing a side out of the one case that should surface nothing; the four-condition `publication` gate was taken as a parameter and **never referenced**, silently reverting a documented decision while its long explanatory comment still sat above the body; and "progress" measured `spot − nearest ATM strike`, a rounding artifact bounded by half a strike step, so on NIFTY it could never leave ±25 and "invalidated" was unreachable. Records why one `ActionLabel` reached by two sources (a real directional read vs. a bare `ACTION_MAP` detection) is the root confusion, why the anchor keys by symbol alone rather than `(symbol, side)`, why progress must be measured *in the surfaced side's favour* or a PE card reads "market up 5" while winning, and why the badge rule had to move out of JSX to be testable at all. **A doc comment is not a gate** — and two harness tests were already failing on the branch, which is how the revert stayed invisible
- [[Gotchas/2026-08-11 - Dhan WebSocket LTT is an IST-based epoch, REST is UTC]] — **read before touching any Dhan timestamp conversion.** Dhan's two surfaces disagree and the docs call both just "EPOCH": the WS binary feed's LTT counts seconds as though IST wall clock were UTC (so it decoded 5h30m into the future), while REST `/charts/historical` is genuine UTC — never share one helper between them. Records why the docs cannot settle it and a session close can (MCX's final tick decoded to `23:29:59Z`, impossible as UTC, exactly the 23:30 IST close as IST), and why the official `DhanHQ-py` client *looks* like it disagrees but doesn't — it formats an IST epoch as UTC and prints only `hh:mm:ss`, recovering the right digits while making no claim about the instant. Blast radius was one JSON field, and **no backfill was needed** because `Quote.updatedAt` is Prisma `@updatedAt`, not `tick.at`. Also: why a round-trip test structurally cannot catch a convention error, and the invariant-shaped regression guard that replaced it
- [[Gotchas/2026-07-23 - Sentinel not working was four stacked config+build faults]] — "Sentinel only shows simulated data" was **four stacked faults**: service stopped (→ web `DEMO` fallback), `SENTINEL_SERVICE_TOKEN` held an `nvapi-` value (401), `MARKET_DATA_FEED=Live` silently means simulated (only `dhan`/`simulated` are valid), and `getCandles` hardwired to the simulator. Plus the `dist/src` vs `dist` stale-`main.js` build-layout trap and the `.tsbuildinfo` cache that masks `tsconfig` changes
- `packages/database/prisma/seed.ts`'s `seedDemoAccount()` throws `bcrypt.hash is not a function` — a pre-existing ts-node ESM/CJS interop bug, confirmed unrelated to any recent change via git diff, not yet fixed. See [[Patterns/2026-07-18 - Market Data Migration 1 executed (Quote revision, baseline established)]].
- No ESLint config exists anywhere in the repo (pre-existing, not caused by any specific change) — confirmed during Market Data Migration 1.
- `prisma migrate dev` refuses to run in a non-interactive/non-TTY shell, even with `--create-only` — hand-author migration SQL from `prisma migrate diff --script` output and apply with `prisma migrate deploy` instead. See [[Patterns/2026-07-18 - Market Data Migration 1 executed (Quote revision, baseline established)]].
- Always diff a proposed Prisma migration against the **live DB** (`prisma migrate diff --from-url`), not just the schema file — schema.prisma can be edited ahead of any migration during a design conversation, silently drifting from what's actually applied.
- Zustand stores with `persist` middleware need `skipHydration: true` + a manual `rehydrate()` in `useEffect` (deterministic seed data, no `Math.random()`/`Date.now()` before rehydration) to avoid SSR/client hydration mismatches. See [[Patterns/2026-07-18 - M3 dockable workspace (zustand store, dock engine, command palette)]].
- Setting `NVIDIA_NIM_BASE_URL` without `NVIDIA_NIM_API_KEY` registers the `nvidia-nim` provider successfully and then 401s on every call ("Header of type `authorization` was missing") — the base URL alone is enough to register because self-hosted NIM needs no key. Run `npm run nim:smoke` in `services/sentinel` to catch it up front. See [[API/2026-07-23 - NVIDIA free tools (NIM API, Agent Skills)]].
- NIM free-tier latency is **not predicted by parameter count** and the catalog never shows it — the *smallest* model tested (`meta/llama-3.2-3b-instruct`) was the slowest and the only outright failure: 2 of 4 prose calls timed out at 90s and it never answered a tool call (240s, 0 bytes), while `meta/llama-3.1-8b-instruct` answers in 0.3s and tool-calls reliably. Always time **and** tool-test a model before pinning it to a tier, especially `fast` — that is where every Sentinel agent, the orchestrator, the brain and `NewsEventClassifier` run. Same note.
- NIM reasoning models return chain-of-thought in `message.reasoning`, not `content`, and spend `max_tokens` on it before writing any visible text — a 60-token budget with `enable_thinking: true` returns a truncated answer. NIM also 400s on `response_format: {type:'json_object'}` (vLLM wants a schema). Same note.
- A mount-only `useEffect` (e.g. in `AppFrame`) does not re-run on client-side route navigation — if "the action succeeded but the shell/UI didn't update," suspect a mount-only effect on a component that doesn't remount on navigation. See [[Patterns/2026-07-18 - M4 Step1 real auth session (sessionStore, AppFrame remount gotcha)]].

## Research
Research summaries and verified findings from trusted external sources.
- [[Research/2026-07-17 - Sentinel Brain audit]] — end-to-end audit: every Brain component (PrismaMemoryStore, PrismaKnowledgeGraph, ConceptLearningEngine, Knowledge Center, pgvector) is fully implemented, not stubbed; the only gaps are operational (no local Postgres/docker-compose/.env yet)
- [[Research/2026-07-17 - Oracle migration assessment]] — BLOCKER: Oracle standardization = full Prisma removal (Prisma has no Oracle provider); no SQL Server exists to replace; pgvector needs Oracle 23ai; decision required before any code
- [[Research/2026-07-18 - Backend audit (Milestone 4 Step 0)]] — only services/api + services/sentinel are real; auth/market-data/notification/tradew-ai/analytics are README-only placeholders; trading-engine (live-money bot) not yet migrated in; full endpoint/DB/Prisma/frontend-client inventory + per-subsystem readiness map
- [[Research/2026-08-10 - Offensive security assessment (auth-bypass class)]] — **read before touching any auth secret or service bind.** CRITICAL JWT dev-fallback forgery (proven account takeover), ADMIN_API_TOKEN was a live Anthropic key, services bound to 0.0.0.0. All fixed via `common/secret-validation.ts` + localhost default bind + constant-time service-token guard; pinned by 3 new spec files. No secrets in git history (working-tree `.env` only)

## Agents
Agent responsibilities and roster notes (Sentinel, TradeW AI, Claude Code subagents), prompt improvements.
- **Where the code actually lives (as of 2026-07-21):** TradeW AI's real agent/RAG/memory/provider logic is implemented in `packages/ai-core` (~1,697 lines) — not in `services/tradew-ai` or `agents/tradew-ai`, which remain README-only stubs. Sentinel's agents (Market & Technical Intelligence, Emotion Intelligence, Trap & Safety Intelligence, Compliance & Audit, Orchestrator) are real and live in `services/sentinel/src/{intelligence,compliance,orchestrator}/`. `agents/sentinel/` holds only a `definitions.json` config file, not source. Full agent roster and "never does" boundaries per agent are specified in `docs/product-architecture/TRADEW-AI.md`, `SENTINEL.md`, and `AGENT-ARCHITECTURE.md` (the cross-cutting naming/consolidation layer) — see [[Plans/2026-07-21 - Full platform and product audit]] for the reconciled summary.

## Plans
Future implementation plans, milestones, strategy documentation.
- [[Plans/2026-08-12 - Admin consolidation auth design (operator boundary)]] — **DECIDED → Option B, backend built** (see [[Decisions/2026-08-12 - Operator identity for the standalone admin console]]). Kept as the reasoning of record. **read before touching `AdminGuard`, `apps/admin`, or moving any page out of `apps/web/src/app/admin`.** Written because a working-tree change was about to make the operator token sufficient **on its own** (`if (!token) { req.user = { isAdmin: true, sub: null }; return true }`), which turns a leaked `.env` into total admin access, deletes the `admin.portal.no_session` detection signal, and — concretely — makes `setAdmin` and `CognitiveProposal.resolvedBy` record `'unknown'`, so the cognition network's only negative training label loses its author. The governing rule: **a shared secret proves a process is trusted, never that a person is present, and never which person.** Two options that both leave `apps/web`'s path untouched — (A) the standalone console acquires a real product JWT server-side, needing *zero* guard changes, cost is JWT refresh in the proxy; (B, recommended) a dedicated `OperatorAccount` + composed `AdminAccessGuard`, cost is a second credential store. Answers the ten auth questions, including the one that decides the architecture: **no header may ever confer trust** — `X-Admin-Proxy`/`X-Forwarded-*` are client-settable, so trust comes from the never-in-a-browser token plus network placement
- [[Plans/2026-08-11 - AI Operating System (eight layers, Phase 1 spine shipped)]] — **read before touching `lib/assistant/` or planning any assistant work.** Product reframe: the AI is an operating layer over the product, not a chat window — eight layers (conversation → planner → execution → chart agent → memory → voice → narration → safety). Spec is binding at `docs/product-architecture/AI-OPERATING-SYSTEM.md`. Ground truth found while scoping: **`packages/ai-core` is real (31 modules) but `services/tradew-ai` is an empty stub** — the gap is the wiring, not the LLM work. Three binding decisions: chart read = structured JSON **and** capture every time (**JSON wins on numbers, disagreement is a bug to log**); brain runs in a real `services/tradew-ai` behind `services/api` per §1; spine before capability. Phase 1 shipped — records the two traps that cost real time: **splitting on bare "and" turns "price of nifty and banknifty" into a navigation away from your screen**, and **a refused clause must poison the whole plan** or "open the chart then buy 50 lots" half-runs. Also why reversible actions are deliberately NOT confirmed (prompt fatigue defeats the gate), and why silent mode never silences it
- [[Plans/2026-08-10 - Production readiness audit (security, scale, infra)]] — **read before touching `infra/`, `services/api/src/common/`, any background `setInterval`, or any Dockerfile.** The reframing finding: the app code handled 500 concurrent users at single-digit-ms p95, while **two of the four Docker images had never successfully built** and four more deployment defects sat behind them — including the quietest one in the repo, Sentinel's citation corpus being absent from its own image (builds, starts, health-checks green, corpus empty, and because uncited verdicts are *dropped* rather than flagged, the flagship feature ships as a silent no-op). Records why leader election (`JobLease`) is a *correctness* mechanism — two replicas meant two matching engines double-filling the same order — and why it is a lease row rather than `pg_advisory_lock`; why the rate limiter's `global` bucket is only global because of one `generateKey` override that looks deletable; Sentinel's ~1.6s/observation and the 1500-VU saturation order (third-party proxy fails first, TradeW's own code returns no 5xx); and the Windows `prisma generate` EPERM / triple-watcher traps that made a passing test suite report 18 phantom failures
- [[Plans/2026-07-21 - Full platform and product audit]] — **start here.** Reconciles the engineering vault, all 21 product-architecture docs, a ground-truth code pass, and LLC-root consolidation history; lists corrections made to stale notes and open decisions still needing the user
- [[Plans/2026-07-17 - Platform audit and implementation roadmap]] — full platform audit (endpoints, auth matrix, DB ownership, security findings, tech debt, production-readiness ratings) + prioritized build order + sprint plan
- [[Plans/2026-07-17 - OCI Free Tier deployment]] — Oracle Cloud (host only, Postgres stays) deployment: Dockerfiles + prod compose + Caddy/SSL + backups + CI/CD for a single Ampere A1 arm64 VM; canonical design in infra/oci/README.md

## API
API integration notes — auth quirks, rate limits, payload shapes, gotchas specific to a third-party API.
- [[API/2026-08-16 - NSE public data (FII-DII, participant OI, breadth) — what Dhan cannot give you]] — **read before any institutional-data feature, before assuming the Dhan token covers a market-data gap, and before giving any agent outbound web access.** **FII/DII is not in Dhan and no token unlocks it** — DhanHQ v2's whole data surface is charts/optionchain/quotes/scrip-masters; a broker publishes what it can execute, an exchange publishes what the market did. NSE serves it free and keyless: `/api/fiidiiTradeReact` (cash), `fao_participant_oi_DDMMYYYY.csv` (Client/DII/FII/Pro contracts), and — the surprise — `/api/allIndices` carries **advances/declines**, the first real breadth this platform has had. Three things make it work: cookie priming (**the prime may 403 and still set usable cookies** — read the jar, ignore the status), a browser-shaped request with a matching Referer, and `getSetCookie()` not `get('set-cookie')`. When NSE refuses it serves an **HTML page with a 200**, so the symptom is `JSON.parse` failing. **The limitation that shapes every UI decision: cash flow and participant OI are END-OF-DAY with no intraday equivalent at any price** — asked at 11am Tuesday, this data is Monday's, so every figure travels with its session. Agent reach is a **closed named-dataset catalogue, never a URL** (`{ask}` never a destination, asserted by test): a caller-supplied URL fetched from inside the network is an SSRF primitive, and fetched page text is data-not-instruction — which is why `event-calendar` drops the company-authored `bm_desc`. Gotchas: `en-IN` uses **lakh grouping** (`2,02,235`); the CSV's headers carry trailing whitespace so match by trimmed name; never coerce `"-"` to 0; and in the assistant's judgement guard **`buy`/`sell` must NOT trigger refusal** (unlike `quotes.ts`) or "did DIIs sell yesterday" is declined because the number is about selling, while `will` must be unqualified. Unofficial surface, no SLA, ToS restricts automated access — mitigated by server-side-only + one shared cache; a data agreement is the durable answer
- [[API/2026-08-11 - Dhan Algo Strategies]] — **read before automating any order placement against Dhan.** The headline: **Dhan has no endpoint that returns algo strategies** — `algos.dhan.co` is a UI-only marketplace of third-party, exchange-approved algos with no API. What exists is one server-side rule engine (**Conditional Triggers**, `/v2/alerts/orders`, API v2.5) whose entire expressible strategy space is the cross-product of 21 fixed-period indicators × 9 operators × 4 timeframes — catalogued here as 14 named archetypes — plus the Tier-3 primitives (`/orders`, `/super/orders`, `/forever/orders`). Records the walls that decide architecture: **Conditional Triggers exclude F&O entirely** (equities/indices only), **one comparison per trigger — no AND/OR**, `frequency: ONCE`, and `trailingJump: 0` silently *cancels* a Super Order's trail. **Static IP whitelisting is mandatory for every order API** (SEBI algo regulation, v2.4) with a 7-day change lock — an infra constraint that cannot be resolved at deploy time; tokens expire in 24h. Also: `algoId` is read-only Dhan attribution, not a strategy id you supply; the docs' lookback limits are more generous than this repo's measured behaviour; and two doc self-contradictions (`comparisonType` enum, a claimed "Exit All API" that no page documents). Reference implementation: `scripts/dhan_algo_strategies.py`
- [[API/2026-07-23 - NVIDIA free tools (NIM API, Agent Skills)]] — **do not re-implement NIM support, it's already in `packages/ai-core`.** Getting Sentinel onto NVIDIA's free tier is 3 env vars + `npm run nim:smoke`; separates the runtime NIM API from the unrelated dev-time NVIDIA Agent Skills; records the ~40 req/min shared ceiling and why swapping embedding models needs no migration

## What does NOT belong here
Live market data, real-time analytics, raw session logs/transcripts, anything already canonically documented in [ARCHITECTURE.md](../ARCHITECTURE.md) or [docs/product-architecture/](../docs/product-architecture/) (link to those instead of duplicating them). Trading-domain memory (market patterns, symbol history) lives in Sentinel's own Postgres Persistent Knowledge Brain (`services/sentinel/src/brain/`), not here.

**Market *concepts*** — what a liquidity sweep is, what confirms a breakout, how FOMO relates to position sizing — belong in `TradeW/knowledge-base/` (YAML, one file per concept, seeded into Postgres and read by Sentinel at runtime), never in this vault. This vault holds knowledge about *building* TradeW; `knowledge-base/` holds knowledge about *markets*. See [[Decisions/2026-07-21 - Sentinel Concept Knowledge Graph (living ontology)]] and `knowledge-base/README.md`.
